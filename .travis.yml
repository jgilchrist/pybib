language: python

python:
    - "2.7"
    - "3.5"
    - "3.6"

install:
    - pip install pipenv
    - pipenv install --dev --ignore-hashes

script: pipenv run nosetests --with-coverage --cover-package=pybib --cover-branches --rednose
